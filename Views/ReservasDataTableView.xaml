<UserControl x:Class="ReservasApp.Views.ReservasDataTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reservas (DataTable)" Style="{StaticResource SectionTitle}"/>

        <DataGrid Grid.Row="1" x:Name="dgReservas"
                  Style="{StaticResource MainDataGrid}"
                  ItemsSource="{Binding Reservas}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ReservaId}" Width="60"/>
                <DataGridTextColumn Header="Aula" Binding="{Binding Aula}" Width="*"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="*"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTextColumn Header="Hora" Binding="{Binding Hora}" Width="90"/>
                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
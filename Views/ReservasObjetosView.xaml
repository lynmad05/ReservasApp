<UserControl x:Class="ReservasApp.Views.ReservasObjetosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reservas (Objetos)" Style="{StaticResource SectionTitle}"/>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,15">
            <StackPanel Orientation="Vertical" Margin="0,0,10,0">
                <TextBlock Text="FECHA" Style="{StaticResource FieldLabel}"/>
                <DatePicker x:Name="dpFecha" Width="160" Height="36"
                            SelectedDate="{Binding FechaFiltro}"/>
            </StackPanel>
            <Button x:Name="btnBuscar" Content="Buscar" Width="100" Height="36"
                    VerticalAlignment="Bottom" Margin="0,0,10,0"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding BuscarCommand}"/>
            <Button x:Name="btnVerTodas" Content="Ver todas" Width="100" Height="36"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding VerTodasCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dgReservas"
                  Style="{StaticResource MainDataGrid}"
                  ItemsSource="{Binding Reservas}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ReservaId}" Width="60"/>
                <DataGridTextColumn Header="Aula" Binding="{Binding NombreAula}" Width="*"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding NombreUsuario}" Width="*"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="110"/>
                <DataGridTextColumn Header="Hora" Binding="{Binding Hora}" Width="90"/>
                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="1.5*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
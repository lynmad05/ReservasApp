<UserControl x:Class="ReservasApp.Views.AulasDataTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Aulas (DataTable)" Style="{StaticResource SectionTitle}"/>

        <DataGrid Grid.Row="1" x:Name="dgAulas"
                  Style="{StaticResource MainDataGrid}"
                  ItemsSource="{Binding Aulas}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AulaId}" Width="80"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Capacidad" Binding="{Binding Capacidad}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
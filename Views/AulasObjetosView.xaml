<Window x:Class="ReservasApp.Views.AulasObjetosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aulas (Objetos)" Height="450" Width="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="txtBuscarNombre" Width="200" Margin="0,0,10,0"
         Text="{Binding FiltroNombre, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Buscar" Width="80" Command="{Binding BuscarCommand}"/>
        </StackPanel>

        <DataGrid x:Name="dgAulas" Grid.Row="1" AutoGenerateColumns="True" IsReadOnly="True" ItemsSource="{Binding Aulas}"/>
    </Grid>
</Window>

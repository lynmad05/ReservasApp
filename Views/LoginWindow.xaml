<Window x:Class="ReservasApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - ReservasApp" Height="280" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Iniciar sesión" FontSize="18" FontWeight="Bold"
                   Grid.Row="0" Margin="0,0,0,15"/>

        <TextBlock Text="Usuario:" Grid.Row="1" Margin="0,0,0,3"/>
        <TextBox x:Name="txtUsername" Grid.Row="1" Margin="0,25,0,10" Height="28"/>

        <TextBlock Text="Contraseña:" Grid.Row="2" Margin="0,0,0,3"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="2" Margin="0,25,0,10" Height="28"/>

        <Button x:Name="btnIngresar" Content="Ingresar" Grid.Row="3"
                Height="32" Margin="0,10,0,0" Click="btnIngresar_Click"/>

        <TextBlock x:Name="txtMensaje" Grid.Row="4" Foreground="Red"
                   Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>